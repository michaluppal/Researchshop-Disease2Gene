# CLAUDE.md - Legacy Claude Code Notes

This project now uses `AGENTS.md` as the active Codex routing file.

The Claude-era rules, commands, and hooks remain in `archive/claude/` as historical context. Do not treat this file as the current source of agent instructions; use:

- `AGENTS.md` for active project routing
- `.codex/rules/` for living project memory
- `.codex/tasks/` for Codex task recipes converted from the old slash commands
- `.codex/hooks/README.md` for notes about the old hook behavior
