% Clean architecture diagram for SoftwareX paper
% Usage: \input{pipeline_tikz} inside a figure* environment
% Or compile standalone: pdflatex pipeline_tikz.tex

\documentclass[border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
\usepackage[T1]{fontenc}

\begin{document}

\begin{tikzpicture}[
    % Base styles
    every node/.style={font=\sffamily},
    box/.style={draw=#1, fill=#1!6, rounded corners=3pt,
                minimum height=1.1cm, align=center, font=\sffamily\small,
                line width=0.6pt},
    box/.default=gray!60,
    domainbox/.style={box=#1, minimum width=2.6cm},
    widedomainbox/.style={box=#1, minimum width=4.0cm},
    smallpill/.style={rounded corners=5pt, draw=gray!40, fill=white,
                      font=\sffamily\tiny, inner sep=2pt, inner ysep=1pt},
    layerlabel/.style={font=\sffamily\scriptsize\bfseries, anchor=north west},
    filelabel/.style={font=\sffamily\tiny\ttfamily, text=gray!50},
    arr/.style={-{Stealth[length=4pt,width=3pt]}, gray!55, thick},
    dasharr/.style={-{Stealth[length=4pt,width=3pt]}, gray!55, thick, dashed},
    node distance=0.5cm and 0.6cm,
]

% ============================================================
%  ROW 1: ELECTRON UI
% ============================================================
\node[domainbox=blue] (query) {
    \textbf{\footnotesize paper\_selection}\\[-1pt]
    Query \& Schema};

\node[domainbox=blue, right=of query] (relevance) {
    \textbf{\footnotesize paper\_selection}\\[-1pt]
    Gene Relevance\\[-2pt]
    Scoring};

\node[box=gray, right=of relevance, minimum width=2.0cm,
      dashed, minimum height=0.9cm, font=\sffamily\scriptsize] (ipc) {
    IPC Bridge};

\node[domainbox=blue, right=4.5cm of relevance] (results) {
    \textbf{\footnotesize output\_writing}\\[-1pt]
    Results Explorer};

% File labels - UI
\node[filelabel, below=1pt of query] {QueryBuilder.tsx};
\node[filelabel, below=1pt of relevance] {geneRelevanceScorer.ts};
\node[filelabel, below=1pt of ipc] {python-bridge.ts};
\node[filelabel, below=1pt of results] {Results.tsx};

% UI background
\begin{scope}[on background layer]
\node[fit=(query)(results), fill=blue!4, draw=blue!25, rounded corners=6pt,
      inner sep=10pt, label={[layerlabel, text=blue!40]north west:
      Electron Desktop UI (TypeScript / React)}] (uilayer) {};
\end{scope}

% UI arrows
\draw[arr] (query) -- (relevance);
\draw[arr] (relevance) -- (ipc);

% ============================================================
%  ROW 2: PYTHON PIPELINE
% ============================================================
\node[domainbox=gray, below=1.4cm of query] (fulltext) {
    \textbf{\footnotesize oa\_filter}\\[-1pt]
    {\scriptsize paper\_reading}\\[-2pt]
    Full-Text
    Acquisition};

\node[domainbox=orange, right=of fulltext] (pubtator) {
    \textbf{\footnotesize candidate\_discovery}\\[-1pt]
    PubTator3 NER\\[-2pt]
    {\tiny precision floor}};

\node[widedomainbox=violet, right=of pubtator] (gemini) {
    \textbf{\footnotesize detail\_extraction}\\[-1pt]
    {\scriptsize Gemini structured extraction}\\[-2pt]
    {\scriptsize + Figure analysis}};

\node[box=gray, right=of gemini, minimum width=2.0cm,
      dashed, minimum height=0.9cm, font=\sffamily\scriptsize] (orch) {
    Worker Pool\\[-2pt]
    {\tiny 2--4 processes}};

% File labels - Pipeline
\node[filelabel, below=1pt of fulltext] {full\_text\_fetcher.py};
\node[filelabel, below=1pt of pubtator] {pubtator\_tool.py};
\node[filelabel, below=1pt of gemini] {modules/paper\_analysis/};
\node[filelabel, below=1pt of orch] {pipeline\_orchestrator.py};

% API pills above pipeline boxes
\node[smallpill, above=3pt of fulltext] {PMC OA \quad Europe PMC};
\node[smallpill, above=3pt of pubtator] {PubTator3 API};
\node[smallpill, above=3pt of gemini] {Gemini Flash API};

% Pipeline background
\begin{scope}[on background layer]
\node[fit=(fulltext)(orch), fill=yellow!4, draw=yellow!40!orange!30,
      rounded corners=6pt, inner sep=10pt, inner ysep=14pt,
      label={[layerlabel, text=yellow!40!black]north west:
      Python Pipeline}] (pipelayer) {};
\end{scope}

% Pipeline arrows
\draw[arr] (ipc.south) -- ++(0,-0.35) -| (fulltext.north);
\draw[arr] (fulltext) -- (pubtator);
\draw[arr] (pubtator) -- (gemini);

% Deterministic seeds (dashed feedback)
\draw[dasharr] (pubtator.south) -- ++(0,-0.35)
    -| node[pos=0.25, below, font=\sffamily\tiny\itshape, text=gray]
       {deterministic candidate seeds} (gemini.south);

% ============================================================
%  ROW 3: VALIDATION FRAMEWORK
% ============================================================
\node[domainbox=green, below=1.4cm of fulltext] (geneval) {
    \textbf{\footnotesize validation}\\[-1pt]
    Gene Validation\\[-2pt]
    {\tiny HGNC (44,943 genes)}};

\node[domainbox=green, right=of geneval] (citeval) {
    \textbf{\footnotesize validation}\\[-1pt]
    Citation\\[-2pt]
    Cross-Reference};

\node[domainbox=green, right=of citeval] (vareval) {
    \textbf{\footnotesize validation}\\[-1pt]
    Variant\\[-2pt]
    Matching};

\node[box=red, right=of vareval, minimum width=2.0cm] (gate) {
    \textbf{\footnotesize Strict Gate}\\[-1pt]
    Confidence $\geq$ 0.7};

% File labels - Validation
\node[filelabel, below=1pt of geneval] {gene\_validator.py};
\node[filelabel, below=1pt of citeval] {SequenceMatcher $\geq$0.85};
\node[filelabel, below=1pt of vareval] {12+ HGVS patterns};

% API pills
\node[smallpill, above=3pt of geneval] {HGNC REST \quad MyGene.info};

% Validation background
\begin{scope}[on background layer]
\node[fit=(geneval)(gate), fill=green!3, draw=green!30,
      rounded corners=6pt, inner sep=10pt, inner ysep=14pt,
      label={[layerlabel, text=green!35!black]north west:
      Validation Framework}] (vallayer) {};
\end{scope}

% Validation arrows
\draw[arr] (gemini.south) -- ++(0,-0.7) -| (geneval.north);
\draw[arr] (geneval) -- (citeval);
\draw[arr] (citeval) -- (vareval);
\draw[arr] (vareval) -- (gate);

% Output: gate -> results
\draw[arr] (gate.east) -- ++(0.4,0) |- (results.south -| orch.east)
    -- (results.south);

% ============================================================
%  LEGEND
% ============================================================
\node[anchor=north west, font=\sffamily\scriptsize\bfseries,
      below=0.6cm of vallayer.south west] (legtitle) {Legend};
\node[anchor=north west, font=\sffamily\tiny, below=1pt of legtitle.south west,
      text width=12cm] {%
    \tikz\fill[blue!6, draw=blue!50, rounded corners=2pt] (0,0) rectangle (0.35,0.2);
    ~Electron UI \quad
    \tikz\fill[orange!6, draw=orange!60, rounded corners=2pt] (0,0) rectangle (0.35,0.2);
    ~PubTator NER (precision floor) \quad
    \tikz\fill[violet!6, draw=violet!50, rounded corners=2pt] (0,0) rectangle (0.35,0.2);
    ~LLM Extraction (recall ceiling) \quad
    \tikz\fill[green!3, draw=green!50!black, rounded corners=2pt] (0,0) rectangle (0.35,0.2);
    ~Validation (anti-hallucination) \quad
    \tikz\fill[red!6, draw=red!60, rounded corners=2pt] (0,0) rectangle (0.35,0.2);
    ~Confidence gate%
};

\end{tikzpicture}

\end{document}
