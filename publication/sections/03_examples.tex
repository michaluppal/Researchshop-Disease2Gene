%%====================================================================
\section{Illustrative Examples}
\label{sec:examples}

\subsection{Research Workflow}

ResearchShop supports a compact literature-curation loop: users provide a PubMed query, author search, or PMID list; select papers; define schema columns such as \emph{Key Finding}, \emph{Disease Association}, \emph{Statistical Evidence}, and \emph{Conclusion}; and receive spreadsheet-ready gene--paper rows with provenance and validation metadata. For each selected open-access paper, the pipeline parses text, tables, captions, figures, and supplementary metadata where available; combines PubTator, HGNC scanning, and LLM candidate discovery; fills the requested schema only for grounded candidates; and exports citation status, NCBI identifiers, candidate source, association grouping, confidence label, and paper metadata. The same backend can be run through the desktop interface or from the command line for scripted analyses.

Figure~\ref{fig:app_workflow_screens} shows the protocol, query/schema, and results views. Table~\ref{tab:demo_output_trace} uses a local demonstration result file to illustrate the visible data model; it is not an accuracy benchmark.

\subsection{Audit Artifacts}

Each run writes the final CSV plus metadata CSV, XLSX, JSON, candidate-audit, drop-debug, and optional trace files. These artifacts record candidate origin, gate outcomes, citation-grounding status, confidence score, fallback paths, and dropped-row reasons. A reviewer can therefore see why a gene appeared, why another was excluded, and whether a supporting sentence was recovered from the paper rather than generated by the LLM. Extraction failures become reproducible records, not silent omissions.

\subsection{Figure and Table Handling}

Many genomics papers encode primary findings in figures, tables, and supplementary files rather than in prose. ResearchShop parses PMC and Europe PMC JATS XML, extracts structured table text, collects figure captions and image URL candidates, and passes table/caption context into evidence validation. When figure analysis is enabled and images are downloadable, a multimodal Gemini model can inspect figures as an optional signal; when images are unavailable, the pipeline degrades to caption and text evidence rather than failing the run.

The parser path uses ResearchShop-owned fetchers for PubMed, PMC, Europe PMC, and citation services. A small adapter uses \texttt{pubmed\_parser} for selected JATS parsing tasks such as body paragraphs and figure metadata, while ResearchShop keeps its own open-access gates, URL probing, CDN fallback, table extraction, supplementary file handling, PubTator integration, and output schema.

\begin{strip}
\vspace{-0.6em}
\centering
\begingroup
\scriptsize
\captionsetup{hypcap=false}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{0.92}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{1.55cm} >{\raggedright\arraybackslash}p{2.15cm} >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}p{2.75cm}@{}}
\toprule
\textbf{Output row} & \textbf{Candidate source} & \textbf{Displayed evidence trail} & \textbf{Audit interpretation} \\
\midrule
\textit{IFNG} & LLM text; deterministic lexicon & Gene-normalized immune-response finding with citation status, confidence label, local HGNC validation, and matched source text. & Inspect source sentence before reuse. \\
\textit{HLA-C} / HLA-C*04 & LLM text & Allele-like input remains in the variant field while the gene label normalizes to an HGNC-compatible symbol. & Triage lead; HLA nomenclature requires expert interpretation. \\
\textit{CXCL10} & PubTator & Sparse evidence is retained with a conservative confidence label and needs-review citation status. & Visible as a paper index, not a final assertion. \\
\bottomrule
\end{tabularx}
\captionof{table}{Illustrative output and audit trail from a local demonstration result file.}
\label{tab:demo_output_trace}
\vspace{0.55em}

\begin{minipage}[t]{0.31\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/app_protocol_selection.png}
        \scriptsize (a) Protocol selection.
\end{minipage}
\hfill
\begin{minipage}[t]{0.31\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/app_query_builder.png}
        \scriptsize (b) Query and schema.
\end{minipage}
\hfill
\begin{minipage}[t]{0.31\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/app_results_explorer.png}
        \scriptsize (c) Results explorer.
\end{minipage}
\captionof{figure}{ResearchShop desktop workflow: protocol selection, paper-analysis setup, and validated results review.}
\label{fig:app_workflow_screens}
\endgroup
\vspace{-0.6em}
\end{strip}

% \subsection{Reproducibility Checks}

% The repository includes offline tests for the main scientific risk points: XML parsing, figure metadata extraction, citation grounding, gene validation, F2-isoprostane false-positive suppression, association-type grouping, PubTator/NCBI caching, per-paper extraction import compatibility, output writing, and pipeline tracing. These checks do not require a Gemini key or network access and are intended to be run before release builds.

% Table~\ref{tab:reproducibility_checks} summarises the validation surfaces covered by the public release artifacts. The table is not an external accuracy benchmark; it documents the reproducible checks and demonstration artifacts that a reader can inspect without access to private audit records.

% \begin{table}[t]
% \centering
% \footnotesize
% \setlength{\tabcolsep}{4pt}
% \begin{tabularx}{\columnwidth}{>{\raggedright\arraybackslash}p{2.1cm} X}
% \toprule
% \textbf{Check surface} & \textbf{Public artifact or exercised behavior} \\
% \midrule
% Parser and acquisition & Offline parser tests exercise PMC/Europe PMC JATS text, tables, captions, metadata, and unavailable-image fallback. \\
% LLM validation & Validation tests and audit/drop artifacts exercise HGNC normalization, grounding, citation anchoring, numeric consistency, and grouping. \\
% False positives & Gene-validation tests cover ambiguous biomedical strings such as F2-isoprostane and clinical-vs-gene abbreviations. \\
% Output writing & Demo files exercise CSV, JSON, XLSX, metadata CSV, pipeline trace, and candidate-audit paths shown in Table~\ref{tab:demo_output_trace} and Figure~\ref{fig:app_results_screen}. \\
% Packaging & Release scripts cover Electron packaging targets and Python dependency bootstrap behavior. \\
% \bottomrule
% \end{tabularx}
% \caption{Public reproducibility and validation surfaces covered by the release artifacts.}
% \label{tab:reproducibility_checks}
% \end{table}
