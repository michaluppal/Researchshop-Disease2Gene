# Pipeline Contract

This is the canonical public contract for the ResearchShop pipeline. It maps the user-facing pipeline domains to the code that owns each step and to the trace node names emitted when tracing is enabled.

Legacy import paths such as `stage5` and `gemini_extractor` are removed from the public release branch. The active per-paper architecture is `pipeline/modules/paper_analysis/` and `PaperAnalysisPipeline`.

## Normalization Boundary

Paper-level normalization is owned by `pipeline/modules/content_preparation.py` during `paper_reading`. It builds `PreparedPaperContent` with preserved raw text plus normalized citation/abstract text, table citation indexes, and deterministic `NormalizationRecord` entries. Those records are currently limited to a small, explicit set of paper-mention bridges: IFNG/TNF/MMP9 aliases and HLA class-I allele forms.

Downstream `paper_analysis` code may use those normalized indexes for candidate seeding, grounding, citation validation, and evidence metadata. This boundary does not rewrite the raw paper text sent to Gemini, does not lower validation thresholds, and does not replace candidate-specific HGNC/variant normalization inside `PaperAnalysisPipeline` and `GeneValidator`.

## Gemini Structured-Output Contract

Gemini calls use Pydantic response schemas from `pipeline/modules/paper_analysis/schemas.py`. Candidate discovery from abstract text, full text, and figures shares one association shape: reported HGNC-style gene, reported variant, original paper/figure mention, and evidence sentence. Detail extraction uses the same typed stack but builds a dynamic response model from the user-requested columns. SDK parsed responses and fallback text-JSON parsing both validate against the same Pydantic model before downstream code ingests rows.

## Output Artifact Contract

The primary output is for researchers reviewing extracted associations. The metadata and debug artifacts are for reproducibility, troubleshooting, and audit.

| Artifact | Contract |
|---|---|
| Primary CSV, JSON, and Excel `Results` sheet | One row per emitted gene-paper association. Columns are limited to `Gene`, `Variant`, requested user fields and their `Citation` pairs, confidence/grouping/provenance fields, extraction/evidence/context visibility fields, and source-paper metadata. |
| Metadata CSV and Excel `Metadata` sheet | Same row order as the primary output, joined by `PMID`, `Gene/Group`, and `Variant Name`. Contains all diagnostic fields, including validation confidence/source, candidate source, citation-validation booleans/details, NCBI metadata, context diagnostics, and gate/debug fields. |
| Excel association-group sheets | Optional convenience views copied from the primary `Results` sheet and split by `Association Group`; they do not introduce extra fields. |
| Candidate audit and drop-debug JSON | Candidate lifecycle, dropped-gate reasons, and run-level forensic data. These are not researcher-facing result tables. |

Primary outputs must not infer public columns by exclusion. Only requested user columns and the fixed researcher-facing fields are promoted to the primary CSV/JSON/Excel `Results` sheet; new diagnostics belong in metadata unless there is an explicit publication/readability reason to surface them.

| Domain | Step | Function name | Input | Output | Description | Failure/skip behavior | Trace node |
|---|---|---|---|---|---|---|---|
| `paper_selection` | Run entry and configuration | `pipeline/run_pipeline.py:main()` | CLI args, `GEMINI_API_KEY`, `ENTREZ_EMAIL`, user columns, optional trace env vars | Parsed run request and initialized config | Validates process-level inputs and calls the Python orchestrator used by Electron. Secrets are read from environment variables. | Missing required config fails before analysis starts. Cancellation returns through the normal progress/log protocol. | None |
| `paper_selection` | Start complete run | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` | Query, explicit PMIDs, author names, user columns, top-N target | Run state, progress/log emitters, output result dict | Owns the top-level domain order and final return payload. | Invalid Gemini call budget raises before network or worker work. Later domain failures either return `None`, emit metadata-only output, or continue with warnings as described below. | None |
| `paper_selection` | PubMed search and metadata | `pipeline/modules/paper_selection.py:select_papers_and_fetch_metadata()` | Query, explicit PMIDs, author names, top-N target | `initial_pmids`, `mandatory_pmids`, `paper_details`, non-OA count | Builds the candidate paper set from explicit PMIDs, author search, and query search, then fetches PubMed metadata. | Empty searches produce empty candidate sets. Explicit/author PMIDs are treated as mandatory where policy allows. Metadata fetch failures reduce available paper details rather than bypassing later gates. | `pubmed_metadata` |
| `oa_filter` | OA policy gate | `pipeline/modules/paper_selection.py:select_papers_and_fetch_metadata()` and `pipeline/modules/full_text_fetcher.py` PMCID lookup | Candidate PMIDs and PubMed metadata | OA-eligible PMIDs and `papers_excluded_not_oa` | Enforces the product rule that automated extraction uses open-access full text only. | PMIDs without accessible OA full text are excluded from full-text extraction; no paywall bypass is attempted. | `pubmed_metadata`, `full_text_fetch` |
| `paper_reading` | Fetch OA full text | `pipeline/modules/paper_reading.py:fetch_oa_full_text_and_pubtator()` / `pipeline/modules/full_text_fetcher.py:run_fetching()` | `paper_details` PMIDs | `content_dict`, `scraped_pmids`, fetch report | Retrieves PMC/Europe PMC full text and structured content for papers that passed OA policy. | If the content dictionary cannot be read, the domain returns `None`. If no full text is available, orchestration emits metadata-only rows. Individual fetch misses are counted in stats. | `full_text_fetch` |
| `paper_reading` | Text cleaning trace | `pipeline/modules/paper_reading.py:fetch_oa_full_text_and_pubtator()` | Fetched paper content | Cleaned content diagnostics | Records the cleaned content state used downstream. | Trace-only node; cleaning failures are represented by missing or low-quality fetched content. | `text_cleaning` |
| `candidate_discovery` | PubTator NER batch | `pipeline/modules/paper_reading.py:fetch_oa_full_text_and_pubtator()` / `PubTatorTool.extract_from_pmids()` | Scraped PMIDs | Per-PMID PubTator gene and variant results | Runs high-precision biomedical NER before per-paper LLM extraction. | If disabled, skipped. If PubTator fails, the run logs a warning and continues without PubTator seeds. PMIDs not returned by PubTator are counted. | `pubtator_ner` |
| `paper_selection` | Citation fetch | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` / `pubmed_data_collector.fetch_citation_counts_with_fallback()` | Scraped PMIDs | Citation records from iCite with Semantic Scholar fallback | Fetches citation counts used for top-N ordering after full-text availability is known. | Missing citation services leave papers with fallback or zero counts; the pipeline continues. | `citation_fetch` |
| `paper_selection` | Paper ordering | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` inline ordering block | Scraped PMIDs, mandatory PMIDs, citation records | `pmids_to_process` | Places mandatory scraped PMIDs first, then remaining scraped papers by citation count. | If there are no scraped PMIDs, the run writes metadata-only rows. | None |
| `paper_selection` | Forensic abstract scoring | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` / `abstract_screener.has_genetic_content()` | Title, abstract, screening threshold | Screening counters and optional forensic decisions | Scores abstracts for diagnostic artifacts only. Python scoring does not silently filter user-selected papers. | If disabled, skipped. If forensic decision collection fails, a warning is logged and all selected papers still proceed. | None |
| `paper_reading` | Prepare per-paper evidence package | `pipeline/modules/paper_reading.py:prepare_paper_inputs()` | `content_dict`, PubMed metadata, PubTator results | Paper text, abstract, figures, tables, `PreparedPaperContent`, PubTator symbols | Builds the worker input package for one PMID, including normalized paper/table indexes. | Missing `paper_text` causes a minimal metadata row for that paper instead of running AI analysis. | None |
| `candidate_discovery` | Worker start | `pipeline/modules/analysis_runner.py:run_pipeline_worker()` | Prepared paper package, user column descriptions | Per-paper records, debug artifact, Gemini call count | Creates one `PaperAnalysisPipeline` instance inside the worker and runs per-paper analysis. | Worker exceptions are caught and returned as error payloads; orchestration records a minimal/error outcome for that paper. | Paper wrapper |
| `paper_reading` | Context validation | `PaperAnalysisPipeline._validate_and_prepare_paper_text()` | Full paper text, abstract, tables, model context settings | Context validation dict and possibly shortened paper text | Validates paper content against model context limits before Gemini calls. | Required. If context validation fails, per-paper analysis returns an empty DataFrame. | `context_validation` |
| `candidate_discovery` | Abstract Gemini candidate discovery | `GeminiClientMixin.extract_gene_names_from_abstract()` | Abstract text | Candidate associations | Optional abstract/title pass for recall when enabled and abstract text is present. | Optional. Disabled or missing abstract skips the step. Failures are not part of the required full-text pass. | `abstract_pass` |
| `candidate_discovery` | Full-text Gemini candidate discovery | `GeminiClientMixin.extract_gene_names(optional=False)` | Full paper text and PubTator seeds | Candidate associations ingested into `candidate_meta` | Mandatory full-text candidate discovery before detail extraction. Empty association output may continue to deterministic and PubTator sources. | Required. API/parsing failure sets `failed_mandatory_fulltext_gemini` and fails that paper analysis. | `fulltext_pass_greedy` |
| `candidate_discovery` | Optional full-text recall pass | `GeminiClientMixin.extract_gene_names(temperature=0.4, optional=True)` | Full paper text | Additional candidate associations | Optional second LLM pass for recall when enabled. | Optional. Disabled by default. Failure logs a warning and keeps first-pass candidates. | `fulltext_pass_recall` |
| `candidate_discovery` | Deterministic HGNC scan | `CandidateMixin.extract_deterministic_candidates()` | Paper text and HGNC symbol/alias data | Deterministic candidate associations | Adds lexicon-based candidate seeds without an LLM call. | Required by the per-paper contract. Empty results are valid. Later corroboration rules may drop unsupported gene-only hits. | `deterministic_scan` |
| `candidate_discovery` | Figure Gemini candidate discovery | `FigureMixin.extract_gene_names_from_figures()` | Figure metadata and downloadable PMC images | Figure-derived candidate associations with original mention and evidence sentence | Optional multimodal candidate discovery from figures. | Optional. Disabled, missing figures, or image failures skip the step without failing the paper. | `figure_analysis` |
| `candidate_discovery` | PubTator merge | `CandidateMixin._ingest_associations(..., source="pubtator")` | PubTator gene symbols for the PMID | Candidate metadata entries | Merges upstream PubTator NER genes into the per-paper candidate set. | Required when PubTator results are present. If no PubTator genes exist, there is nothing to merge. | `pubtator_merge` |
| `candidate_discovery` | Candidate metadata snapshot | `PaperAnalysisPipeline._run_candidate_discovery()` | Candidate metadata from all sources | Candidate source counts and candidate map | Completes candidate discovery and records source provenance. | Empty candidate sets are valid and may lead to empty downstream output. | `candidate_meta` |
| `validation` | Grounding check | `PaperAnalysisPipeline._run_grounding_check()` | Candidate metadata, paper text, figure evidence | Grounded candidate associations | Drops candidates not found in fetched paper text or accepted figure text. | Required safeguard. Ungrounded candidates are dropped; the paper can continue with remaining candidates. | `grounding_check` |
| `validation` | HGNC validation and normalization | `PaperAnalysisPipeline._run_validation_and_normalize()` / `GeneValidator.validate_associations()` | Grounded candidate associations | Canonical genes, validation results, normalized candidates | Resolves candidate genes to HGNC/local validation and normalizes gene/variant values. | Required safeguard. Invalid or duplicate associations are dropped before detail extraction. | `hgnc_validation` |
| `validation` | Low-confidence gate | `PaperAnalysisPipeline._run_validation_and_normalize()` inline low-confidence branch | HGNC validation results | Candidate drops for weak gene resolutions | Removes associations below the candidate-level validation threshold. | Required when low-confidence candidates exist. Dropped candidates are recorded for debug artifacts. | `low_confidence_gate` |
| `validation` | Corroboration gate | `PaperAnalysisPipeline._run_validation_and_normalize()` inline corroboration branch | Candidate sources, association type, paper context | Candidate drops or deterministic-context rescues | Rejects uncorroborated deterministic gene-only candidates unless context rescue passes. | Required for deterministic-only gene hits. PubTator and LLM-supported candidates count as trusted corroboration. | `corroboration_gate` |
| `detail_extraction` | Detail extraction Gemini call | `GeminiClientMixin.extract_gene_info()` | Validated associations, paper text, user column schema | Row dictionaries for requested fields | Extracts researcher-facing fields and citation fields for validated candidates. | Required when validated associations exist. Empty detail output can produce association-only skeleton rows; transport failures are surfaced in worker/debug output. | `detail_extraction` |
| `detail_extraction` | Merge duplicate rows | `EvidenceMixin._merge_duplicate_gene_rows()` | Detail extraction rows | Consolidated row list | Combines duplicate rows for the same gene/variant when fields can be merged. | Skipped when there are zero or one rows. | `row_merge` |
| `detail_extraction` | Evidence backfill | `EvidenceMixin._backfill_sparse_row_evidence()` | Detail rows, paper text, column descriptions | Rows with sparse evidence fields backfilled | Fills missing evidence/citation fields from grounded text snippets where possible. | Skipped when there are no extracted or fallback rows. Does not add new candidates. | `evidence_backfill` |
| `validation` | Validation and provenance metadata | `MetadataMixin._add_validation_metadata()` / `_add_candidate_provenance_metadata()` | Detail DataFrame and candidate metadata | DataFrame with confidence, source, association, and gate metadata | Adds validation/provenance fields consumed by outputs and audit artifacts. | Required before strict gate. Missing confidence later causes strict gate to drop all rows defensively. | None |
| `validation` | Strict validation gate | `PaperAnalysisPipeline._run_post_validation()` | DataFrame with `validation_confidence` | Filtered DataFrame and strict gate drops | Applies the final medical-accuracy threshold, currently `FINAL_VALIDATION_MIN_CONFIDENCE=0.7`. | Required when enabled. Rows below threshold are dropped. If confidence is missing while enabled, all rows are dropped defensively. | `strict_gate` |
| `validation` | Citation validation | `MetadataMixin._add_citation_validation_metadata()` | DataFrame and normalized paper/table text | Citation validity metadata | Checks extracted citation text against normalized evidence. | Skipped when disabled or when the DataFrame is empty. | `citation_validation` |
| `validation` | Evidence gate | `EvidenceMixin._apply_evidence_gate()` | DataFrame and user column descriptions | Evidence-filtered DataFrame and evidence gate drops | Drops rows lacking sufficient grounded evidence in required fields. | Required post-validation step. Empty output is allowed and triggers metadata-only/no-validated-gene handling upstream. | `evidence_gate` |
| `validation` | Context metadata | `MetadataMixin._add_context_metadata()` | Final per-paper DataFrame and context validation dict | DataFrame with context/truncation metadata | Records context-window status and truncation diagnostics on emitted rows. | Skipped when the final per-paper DataFrame is empty. | None |
| `output_writing` | Finalize worker result | `pipeline/modules/result_enrichment.py:finalize_paper_result()` | Worker payload, PubMed metadata, citation records, PubTator results | Per-paper DataFrame and debug artifact | Adds paper metadata, citations, PubTator/NCBI IDs, figure counts, and debug fields to worker rows. | Worker errors or empty records become minimal/error rows where possible; debug artifacts retain failure status. | None |
| `output_writing` | Accumulate run rows | `pipeline/modules/result_enrichment.py:accumulate_result()` | Per-paper DataFrame, run stats | `all_results_df`, counters, collected PMIDs | Appends finalized paper rows and updates run-level extraction statistics. | Empty per-paper DataFrames are skipped, with minimal rows preserved separately. | None |
| `validation` | NCBI Gene enrichment | `NCBIGeneTool.enrich_gene_symbols()` | Emitted gene symbols and available NCBI Gene IDs | NCBI gene metadata map | Adds gene metadata such as full name, aliases, biotype, chromosome, and missing NCBI Gene IDs. | Optional. If disabled or if enrichment fails, the run logs a warning and continues without NCBI metadata. | `ncbi_enrichment` |
| `output_writing` | Top-N and metadata-only policy | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` inline policy block | Full rows, minimal rows, `top_n_cited` | Selected full-review rows plus metadata-only rows | Keeps up to the requested number of full-review PMIDs and appends metadata-only rows for papers without final genes. | If no validated genes are extracted, the run writes metadata-only/no-validated-gene rows. | None |
| `output_writing` | Deduplicate rows | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` inline groupby block | Final candidate rows | Deduplicated DataFrame with aggregated variant names | Merges duplicate rows with identical user-facing fields and aggregates variant names. | Errors are logged and deduplication is skipped. | None |
| `output_writing` | Column order | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` inline column-order block | Deduplicated DataFrame and user columns | Ordered final DataFrame | Places gene/provenance columns first, user columns next, and diagnostics last. | Missing optional columns are ignored. | None |
| `output_writing` | Write primary outputs and audit artifacts | `pipeline/modules/pipeline_orchestrator.py:_write_split_output()` plus nested audit/debug writers | Final ordered DataFrame and output paths | Primary CSV, metadata CSV, XLSX, JSON, candidate audit JSON, drop-debug JSON | Writes the researcher-facing output files and forensic artifacts. | Output write exceptions fail the run. Candidate audit/drop-debug paths are included when successfully written. | `output_writer` |
| `output_writing` | Return Electron result | `pipeline/modules/pipeline_orchestrator.py:run_complete_pipeline()` and `pipeline/run_pipeline.py:main()` | Output paths and optional warning/trace path | `RESULT:{json}` stdout payload | Returns file paths and run warnings to the Electron bridge. | If quota warnings or trace output exist, they are included in the result dict. | None |
